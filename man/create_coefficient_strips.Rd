% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{create_coefficient_strips}
\alias{create_coefficient_strips}
\title{Create coefficient strip plot for per-sample reproducibility}
\usage{
create_coefficient_strips(
  coef_results,
  meta_results,
  cell_type,
  output_path,
  fdr_threshold = 0.05,
  n_top = 20
)
}
\arguments{
\item{coef_results}{A \code{data.table} with per-sample coefficient
results. Must contain columns \code{gene}, \code{coef}, \code{se},
and \code{sample_id}.}

\item{meta_results}{A \code{data.table} with per-gene meta-analysis
results. Must contain \code{gene}, \code{combined_coef}, and an FDR
column (\code{fisher_fdr} or \code{fdr}).}

\item{cell_type}{Character. Cell type name for the plot title.}

\item{output_path}{Character. File path to save the plot.}

\item{fdr_threshold}{Numeric. Significance threshold (default: 0.05).}

\item{n_top}{Integer. Maximum number of top genes to plot (default: 20).}
}
\value{
The \code{ggplot} object invisibly, or invisible \code{NULL} if
no significant genes.
}
\description{
Shows per-sample coefficients for the top significant genes, allowing
visual assessment of effect consistency across biological replicates.
Genes are ordered by combined coefficient magnitude.
}
\examples{
\dontrun{
create_coefficient_strips(
  coef_results = per_sample_coefs,
  meta_results = meta_results,
  cell_type = "LEC",
  output_path = "coefficient_strips.pdf"
)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_loading.R
\name{load_metadata_only}
\alias{load_metadata_only}
\title{Load just metadata and coordinates (fast, no expression matrix)}
\usage{
load_metadata_only(
  input,
  celltype_column = "cell_type",
  sample_column = "sample_id",
  x_column = NULL,
  y_column = NULL
)
}
\arguments{
\item{input}{Either a path to an \code{.rds} file or an in-memory Seurat,
SingleCellExperiment, or SpatialExperiment object.}

\item{celltype_column}{Character. Name of the metadata column containing cell
type annotations (e.g., \code{"cell_type"}).}

\item{sample_column}{Character. Name of the metadata column containing
sample/replicate IDs (default: \code{"sample_id"}).}

\item{x_column}{Character or NULL. Name of X coordinate column. If NULL,
auto-detected via \code{\link{get_coord_columns}}.}

\item{y_column}{Character or NULL. Name of Y coordinate column. If NULL,
auto-detected via \code{\link{get_coord_columns}}.}
}
\value{
A \code{data.table} with cell metadata including columns \code{barcode},
\code{x}, \code{y}, and the cell type and sample columns.
}
\description{
Extracts cell metadata and spatial coordinates from a RIPPLE-compatible
input (Seurat, SingleCellExperiment, SpatialExperiment, or an \code{.rds} file
containing one of these) without keeping the full expression matrix in
memory.
}
\examples{
\dontrun{
meta <- load_metadata_only(
  input = "/path/to/seurat.rds",
  celltype_column = "cell_type",
  sample_column = "sample_id"
)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{create_gradient_volcano}
\alias{create_gradient_volcano}
\title{Create gradient volcano plot for distance-expression analysis}
\usage{
create_gradient_volcano(
  results,
  cell_type,
  output_path,
  fdr_threshold = 0.05,
  query_label = "Query",
  k_neighbors = 1
)
}
\arguments{
\item{results}{A \code{data.table} with per-gene meta-analysis results.
Must contain \code{gradient_score}, \code{gene}, and an FDR column
(\code{fisher_fdr} or \code{fdr}). Optionally contains
\code{decay_pattern} for coloring.}

\item{cell_type}{Character. Cell type name for the plot title.}

\item{output_path}{Character. File path to save the plot (e.g., a PDF).}

\item{fdr_threshold}{Numeric. Significance threshold (default: 0.05).}

\item{query_label}{Character. Display label for the query cell type
(default: "Query").}

\item{k_neighbors}{Integer. Number of nearest neighbors used in the
analysis, shown in the title (default: 1).}
}
\value{
The \code{ggplot} object, invisibly.
}
\description{
Creates a volcano plot showing genes colored by decay pattern and sized by
significance. Uses \code{gradient_score} on the x-axis and
\code{-log10(FDR)} on the y-axis, with the top significant genes labeled.
}
\examples{
\dontrun{
create_gradient_volcano(
  results = meta_results,
  cell_type = "LEC",
  output_path = "volcano.pdf",
  query_label = "Tumor"
)
}

}

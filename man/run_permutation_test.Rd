% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.R
\name{run_permutation_test}
\alias{run_permutation_test}
\title{Single-gene permutation test}
\usage{
run_permutation_test(
  counts,
  coords_target,
  coords_all,
  sample_ids,
  n_perms = 500,
  observed_coef,
  sample_ids_all,
  query_per_sample,
  k_neighbors = 1,
  total_counts,
  max_distance = 200,
  min_cells_per_sample = 30,
  min_expr_cells = 5
)
}
\arguments{
\item{counts}{Integer vector of raw transcript counts for target cells.}

\item{coords_target}{Numeric matrix (n_target x 2). Coordinates of target
cells.}

\item{coords_all}{Numeric matrix (n_all x 2). Coordinates of ALL cells
(for drawing pseudo-query cells from).}

\item{sample_ids}{Character vector (length n_target). Sample IDs for each
target cell.}

\item{n_perms}{Integer. Number of permutations (default: 500).}

\item{observed_coef}{Numeric. The observed combined coefficient from the
real analysis.}

\item{sample_ids_all}{Character vector (length n_all). Sample IDs for all
cells.}

\item{query_per_sample}{Named integer vector. Number of query cells per
sample (names = sample IDs).}

\item{k_neighbors}{Integer. Number of nearest neighbors for distance
calculation (default: 1).}

\item{total_counts}{Numeric vector (length n_target). Total counts per
target cell (for Poisson offset).}

\item{max_distance}{Numeric. Maximum distance to consider in um
(default: 200).}

\item{min_cells_per_sample}{Integer. Minimum target cells per sample
(default: 30).}

\item{min_expr_cells}{Integer. Minimum expressing cells for GLM fit
(default: 5).}
}
\value{
A list with:
\describe{
\item{\code{null_coefs}}{Numeric vector of null distribution coefficients.}
\item{\code{perm_pval}}{Numeric. Two-sided empirical p-value.}
}
}
\description{
Validates a distance-expression gradient by shuffling query cell labels
within each sample and recalculating the distance-expression coefficient.
Uses stratified sampling to preserve per-sample query cell counts.
}
\details{
For each permutation:
\enumerate{
\item Randomly sample pseudo-query cells WITHIN each sample (stratified),
preserving the original query cell count per sample.
\item Compute distances from all target cells to the nearest pseudo-query cell.
\item Fit per-sample Poisson GLMs and combine via inverse-variance weighting.
}

The empirical p-value is calculated as:
\code{(sum(|null| >= |observed|) + 1) / (n_valid_perms + 1)}
}
\examples{
\dontrun{
result <- run_permutation_test(
  counts = rpois(100, 5),
  coords_target = matrix(runif(200), ncol = 2),
  coords_all = matrix(runif(2000), ncol = 2),
  sample_ids = rep(c("s1", "s2"), each = 50),
  n_perms = 100,
  observed_coef = -0.005,
  sample_ids_all = rep(c("s1", "s2"), each = 500),
  query_per_sample = c(s1 = 50, s2 = 60),
  k_neighbors = 1,
  total_counts = rpois(100, 5000)
)
}

}

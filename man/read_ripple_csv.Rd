% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_input.R
\name{read_ripple_csv}
\alias{read_ripple_csv}
\title{Read a RIPPLE input from a CSV directory}
\usage{
read_ripple_csv(dir_path, output_class = c("SpatialExperiment", "Seurat"), ...)
}
\arguments{
\item{dir_path}{Path to a directory containing:
\describe{
\item{counts.csv}{First column = gene names, remaining columns = cells.}
\item{metadata.csv}{First column = cell barcodes, remaining columns =
metadata fields.}
\item{coords.csv (optional)}{First column = cell barcodes, two additional
columns for x and y coordinates.}
}}

\item{output_class}{Character. Passed to \code{\link{make_ripple_input}}.}

\item{...}{Additional arguments forwarded to \code{\link{make_ripple_input}}.}
}
\value{
A \code{SpatialExperiment} or \code{Seurat} object.
}
\description{
Convenience wrapper that loads counts, metadata, and coordinates from CSV
files in a directory, then calls \code{\link{make_ripple_input}} to build
a canonical object.
}
\examples{
\dontrun{
spe <- read_ripple_csv("/path/to/csv_dir", output_class = "SpatialExperiment")
results <- run_ripple(spe,
  query_celltype = "Tumor",
  celltype_column = "cell_type"
)
}

}

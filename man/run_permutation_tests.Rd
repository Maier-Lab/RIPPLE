% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.R
\name{run_permutation_tests}
\alias{run_permutation_tests}
\title{Run permutation tests for multiple genes}
\usage{
run_permutation_tests(
  genes,
  count_matrix,
  target_barcodes,
  coords_target,
  coords_all,
  sample_ids_target,
  sample_ids_all,
  query_per_sample,
  observed_coefs,
  n_perms,
  k_neighbors,
  max_distance_um,
  min_cells_per_sample,
  min_expr_cells,
  total_counts_target
)
}
\arguments{
\item{genes}{Character vector. Gene names to test.}

\item{count_matrix}{Sparse or dense matrix. Raw count matrix (genes x cells).}

\item{target_barcodes}{Character vector. Barcodes of target cells.}

\item{coords_target}{Numeric matrix (n_target x 2). Coordinates of target
cells.}

\item{coords_all}{Numeric matrix (n_all x 2). Coordinates of ALL cells.}

\item{sample_ids_target}{Character vector. Sample IDs for target cells.}

\item{sample_ids_all}{Character vector. Sample IDs for all cells.}

\item{query_per_sample}{Named integer vector. Number of query cells per
sample.}

\item{observed_coefs}{Named numeric vector. Observed combined coefficients
per gene (names = gene names).}

\item{n_perms}{Integer. Number of permutations per gene.}

\item{k_neighbors}{Integer. Number of nearest neighbors for distance
calculation.}

\item{max_distance_um}{Numeric. Maximum distance in micrometers.}

\item{min_cells_per_sample}{Integer. Minimum target cells per sample for
GLM fitting.}

\item{min_expr_cells}{Integer. Minimum expressing cells for GLM fitting.}

\item{total_counts_target}{Numeric vector. Total UMI counts per target cell
(for Poisson offset).}
}
\value{
A \code{data.table} with columns \code{gene} and \code{perm_pval}.
}
\description{
Batch version of \code{\link{run_permutation_test}} that loops over a vector
of genes and returns a \code{data.table} of gene-level permutation p-values.
Used internally by \code{\link{run_ripple}} to validate the top significant
distance-expression gradients.
}
\examples{
\dontrun{
perm_dt <- run_permutation_tests(
  genes = c("Cxcl12", "Ccl21a"),
  count_matrix = counts,
  target_barcodes = barcodes,
  coords_target = target_xy,
  coords_all = all_xy,
  sample_ids_target = target_samples,
  sample_ids_all = all_samples,
  query_per_sample = c(s1 = 50, s2 = 60),
  observed_coefs = c(Cxcl12 = -0.005, Ccl21a = -0.003),
  n_perms = 500,
  k_neighbors = 1,
  max_distance_um = 200,
  min_cells_per_sample = 30,
  min_expr_cells = 5,
  total_counts_target = total_counts
)
}

}

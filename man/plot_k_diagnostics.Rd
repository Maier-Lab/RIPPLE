% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_k_diagnostics}
\alias{plot_k_diagnostics}
\title{Plot k-selection diagnostics for choosing k_neighbors}
\usage{
plot_k_diagnostics(
  input,
  query_celltype,
  celltype_column,
  sample_column = "sample_id",
  k_range = 1:10,
  max_distance_um = 200,
  x_column = NULL,
  y_column = NULL,
  verbose = TRUE
)
}
\arguments{
\item{input}{A Seurat, SCE, or SpatialExperiment object, or a path to an
\code{.rds} file.}

\item{query_celltype}{Character. Query cell type label.}

\item{celltype_column}{Character. Cell type column name.}

\item{sample_column}{Character. Sample column (default: \code{"sample_id"}).
Diagnostics are computed per-sample then averaged.}

\item{k_range}{Integer vector of k values to evaluate (default: \code{1:10}).}

\item{max_distance_um}{Numeric. Distance cap (default: 200).}

\item{x_column}{Character or NULL. X coordinate column (default: auto-detect).}

\item{y_column}{Character or NULL. Y coordinate column (default: auto-detect).}

\item{verbose}{Logical. Print progress (default: TRUE).}
}
\value{
A \code{patchwork} object with two panels. The underlying summary
data is returned invisibly as a \code{data.table} with columns: k,
cell_type, mean_dist, sd_dist.
}
\description{
Generates two diagnostic plots to help users choose an appropriate k value
for the \code{k_neighbors} parameter in \code{\link{run_ripple}}:
\enumerate{
\item Mean distance to the k nearest query cells vs k, per target cell type
\item Standard deviation of that distance vs k, per target cell type
}
}
\details{
At low k, the distance to the nearest query cell is noisy (sensitive to
one stray query cell). As k increases, the mean distance stabilizes. The
"elbow" where the SD flattens suggests a reasonable k choice. Cell types
closer to the query population will have lower mean distances at all k.
}
\examples{
\dontrun{
data(ripple_mock_data)
plot_k_diagnostics(
  ripple_mock_data,
  query_celltype = "Tumor",
  celltype_column = "cell_type"
)
}

}
